# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files - NEVER commit .env with secrets
.env
.env.local
.env.production
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Security
*.pem
*.key
*.crt
.deploy-manifest

# Backups (never commit — too large, contains live site snapshot)
backup-*.tar.gz
backup-LIVE-*/

# Build artifacts shipped to the server — never commit these
*.tar.gz
*.tgz
*.zip
dist-upload*

# Local AdSense relaunch working files (article payloads, scripts)
_adsense_work/
_tt_nginx*.conf

vite.config.ts.timestamp-*.mjs
